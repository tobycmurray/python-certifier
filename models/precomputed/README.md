# Precomputed norms and verified references (committed — precious)

Spectral-norm / margin-Lipschitz computation over these models is very expensive
in exact rational arithmetic (tens of minutes for MNIST, **days** for CIFAR-10),
so the results are committed here rather than regenerated. They are keyed by the
SHA-256 hash of the corrected model `.txt` (see `../README.md`), which is
deterministic, so a committed file stays valid as long as the `.txt` is
regenerated by the same generator.

Two kinds of file per (model, gram-iteration):

- `<hash>.<gram>.norms.json` — the **python-certifier**'s computed norms
  (`op2_norms`, `op2_abs_norms`, etc.). This is the file `robust_certifier.py`
  loads (it looks for `<hash>.<gram>.norms.json` in its working directory, so to
  use a committed one, copy it into the run directory — e.g. `tests/` — first).
- `dafny_<model>_gram<gram>.json` — the **verified Dafny certifier**'s output
  over the *same* corrected model, used as the trusted cross-check by
  `check_margin_lipschitz_bounds`. python and Dafny agreeing on these norms is
  what makes the corrected-model certification "sound *and* verified".

## Current contents

| file | model | gram | source |
|---|---|---|---|
| `2a331148….20.norms.json` | MNIST (corrected) | 20 | python-certifier |
| `dafny_mnist_gram20.json` | MNIST (corrected) | 20 | verified Dafny certifier |

These supersede the old `tests/06ab2a3….norms.json` / `results_…gram_20.json`,
which were computed over Tobler et al.'s 5-decimal-place model.

> Note: cexs are generated against a model's margin-Lipschitz constants, so they
> must be regenerated whenever these norms change (the cex `L` must equal the
> certifier `L`, per model). See the project notes.
